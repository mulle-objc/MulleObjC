language: c

dist: trusty
sudo: required

#
# this is stupid, because we depend on mulle-objc-developer here but
# mulle-objc-developer also tests with MulleObjC
# The proper solution would be to do the mulle-objc-developer tests
# here...
#
before_install:
  - wget -q -O- https://raw.githubusercontent.com/{{PUBLISHER}}/mulle-objc-developer/release/install-ubuntu.sh | sudo TRACE=YES APTFLAGS=-y sh

script:
   - mulle-install -vv -V --prefix /tmp
   - mulle-test -v -V
