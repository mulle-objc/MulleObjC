# DO NOT EDIT THIS FILE. EDIT THE TEMPLATE "templates/.travis.yml.scion"
language: c

dist: trusty
sudo: required

before_install:
  - wget -q -O- https://raw.githubusercontent.com/mulle-nat/mulle-objc-developer/release/install-ubuntu.sh | sudo TRACE=YES APTFLAGS=-y sh

script:
   - mulle-install -vv -V  --prefix /tmp
   - mulle-test -v -V
